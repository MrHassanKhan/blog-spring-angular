version: '2'
services:
  blogspringangularapp-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/BlogSpringAngularApp/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=BlogSpringAngularApp
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
